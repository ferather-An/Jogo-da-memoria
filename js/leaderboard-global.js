// leaderboard-global.js - Placar Global (Firestore)
// Este arquivo usa as funções do firebase-init.js:
// - window.loadGlobalLeaderboard(level)
// - window.saveGlobalScore(level, time, attempts)

// As funções estão definidas em firebase-init.js e são acessadas via window.
// Exemplo de uso:

// Para carregar:
// const scores = await window.loadGlobalLeaderboard('classic');

// Para salvar:
// await window.saveGlobalScore('classic', 120, 15);
